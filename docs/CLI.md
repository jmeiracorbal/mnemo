# CLI reference

The current command reference lives in the [mnemo wiki](https://github.com/jmeiracorbal/mnemo/wiki/CLI-Reference).

Run `mnemo --help` or `mnemo <command> --help` for the interface of your installed binary.
